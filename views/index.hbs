//Display different information in index depending on the role the user have
{{#if fName}}
    {{#if admin}}

        {{>_adminIndex}}

    {{/if}}

    {{#if teacher}}

        {{>_teacherIndex}}

    {{/if}}

    {{#if student}}

        {{>_studentIndex}}

    {{/if}}

{{else}}

    {{>_adminIndex}}
{{/if}}
